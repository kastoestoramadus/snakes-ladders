# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                         controllers.GameController.index
GET     /reset-game               controllers.GameController.resetTheGame(players: scala.collection.Seq[java.lang.String], computersNo: scala.Int)
GET     /make-move                controllers.GameController.makeNextMove()
GET     /players-positions        controllers.GameController.playersPositions
GET     /is-finished              controllers.GameController.isFinished
GET     /next-moves               controllers.GameController.nextMoves
GET     /board                    controllers.GameController.board

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

